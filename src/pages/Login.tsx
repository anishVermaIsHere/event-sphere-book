import Login from "../components/login";
import Section from "@/components/common/section";

const LoginPage = () => {
  return (
    <Section classes="flex justify-center">
      <Login />
    </Section>
  );
};

export default LoginPage;
